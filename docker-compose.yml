version: "3.8"

services:
  vector:
    image: denisitpro/go-example:test1
    container_name: go-test
    hostname: go-test
    network_mode: host
    logging:
      driver: json-file
      options:
        max-size: '100m'
        max-file: '2'
